//Metodo includes
// Verifica se o elemento existe no array

let numeros = [1, 2, 3, 4, 5, 6, 7, 8];

console.log(numeros.includes(6)); 

// vai aparecer que é verdadeiro, pois existe o número 6 no array

console.log(numeros.includes(9));

// Vai aparecer que é falso, pois o número 9 não existe no array


let desenhos = ['barbie', 'spirit o corcel indomável', 'cinderela', 'rapunzel', 'pica pau', 'encanto', 'elementos', 'fronzen'];

console.log(desenhos.includes('cinderela'));

console.log(desenhos.includes('moana'));
