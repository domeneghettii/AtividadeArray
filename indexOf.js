// Metodo indexOf()
//  Retorna o primeiro índice no elemento encontrado no array

let doces = ['chocolate', 'paçoca', 'pirulito', 'bala'];

let indiceBala = doces.indexOf('paçoca');

console.log(indiceBala);

// aparece o 1, pois o elemento 1 é a 'paçoca' que está dentro do array


let indiceAlfajor = doces.indexOf('alfajor');

console.log(indiceAlfajor);

// aparece -1, pois o elemento 'alfajor' bnão está dentro do array

